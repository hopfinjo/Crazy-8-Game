"use strict";

/**
 * The object that represents the current state of the game.
 * Calling the <code>play()</code> method on this object
 * launches the game code.
 */
var presenter = new Presenter();

// Play one round of Crazy Eights against the computer
presenter.play();
